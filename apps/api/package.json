{
  "name": "@club/api",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env node_modules/.bin/tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit false --outDir dist",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@club/database": "workspace:*",
    "@club/schemas": "workspace:*",
    "@fastify/cors": "^11.1.0",
    "fastify": "^5.6.0",
    "mongodb": "^6.20.0",
    "xlsx": "^0.18.5",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
